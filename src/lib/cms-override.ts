/**
 * Re-export from core — kept for backward compatibility.
 * Prefer importing from '@/core/lib/seo/cms-override' directly.
 */
export {
  getCmsOverride,
  getPageCmsOverride,
  type CmsOverride,
  type CmsOverrideSEO,
} from '@/core/lib/seo/cms-override';
